apiVersion: v1
kind: Pod
metadata:
  name: worker-app-pod
  labels:
    name: worker-app-pod
    author: chi
spec:
  containers:
    - name: worker-app
      image: dockersamples/examplevotingapp_worker
...

